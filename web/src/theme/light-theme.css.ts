import { createTheme } from "@vanilla-extract/css";
import { vars } from "./contract.css";

// dark theme is prio, will go ahead and update this theme once
// all the vars are more finalized
export const lightThemeClass = "";
