version: "3.9"
services:
  api:
    build: api
    ports:
      - "8080:8080"
    volumes:
      - "./api:/app/"
      - "api_target:/app/target"
  frontend:
    build: frontend
    ports:
      - "3000:3000"
    volumes:
      - "./frontend:/app/"
volumes:
  api_target:
